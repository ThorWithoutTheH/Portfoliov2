import React, { Component } from "react";
import styled from "@emotion/styled";

const WorkContainer = styled.div``;

function FlyCasual() {
  return <WorkContainer>Fly Casual</WorkContainer>;
}

export default FlyCasual;
